#include "main.h"

/**
 * print_diagaonals- here is a function
 *
 * @n: description
 *
 * Return: 0
 */

void print_diagonal(int n);
{
	int space;
	int line;

	if (n <= 0)
	{
		_putchar('\n)
	}
	else
	{
		for (space = 0; space < n; space++)
			_putchar(32);	
	}
}
