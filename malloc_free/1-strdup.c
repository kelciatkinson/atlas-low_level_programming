#include "main.h"

/**
 * _strdup- here is a description
 *
 * @str: here is a parameter
 *
 * Return: char
 */

char *_strdup(char *str)
{
	char *newStr;
	int i;
	
	if (str == NULL)
		return (NULL);
	newStr = malloc(sizeof(str));
}
