#include "3-calc.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * op_add- here is a description
 *
 * @a: here is a parameter
 * @b: here is another parameter
 *
 * Return: a + b
 */

int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub- here is a description
 *
 * @a: here is a parameter
 * @b: here is another parameter
 * 
 * Return: a - b
 */

int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul- here is a description
 *
 * @a: here is a parameter
 * @b: here is another parameter
 *
 * Return: a * b
 */

int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div- here is a description
 *
 * @a: here is a parameter
 * @b: here is another parameter
 *
 * Return: a / b
 */

int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod- here is a description
 *
 * @a: here is a parameter
 * @b: here is another parameter
 *
 * Return: remainder
 */

int op_mod(int a, int b)
{
	return (a % b);
}
