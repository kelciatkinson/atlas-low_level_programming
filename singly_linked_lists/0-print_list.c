#include "lists.h"

/**
 * print_list- here is a description
 *
 * @h: here is a parameter
 *
 * Return: # of nodes
 */

size_t print_list(const list_t *h)
{
	int num_nodes = 0;

	if (*str == NULL)
	{
		printf("[0] (nil)");
	}
	printf("");
	return (num_nodes);
}
