#include "main.h"

/**
 * is_prime_number- here is a function
 *
 * @n: here is a parameter
 *
 * Return: 0
 */

int is_prime_number(int n)
{
	if (n % 2 == 1)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
