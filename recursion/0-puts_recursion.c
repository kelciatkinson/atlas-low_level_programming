#include "main.h"

/**
 * _puts_recursion- here is a description
 *
 * @s: here is a parameter
 *
 * Return: void
 */

void _puts_recursion(char *s)
{
	int i;

	if (s[i] == '\0')
	{
		return;
	_putchar(%c, s[i]);
	i++;
	}
}
